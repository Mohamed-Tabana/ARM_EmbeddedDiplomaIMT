
#include "SPI_int.h"

void SPI_vInit()
{

}
uint16_t SPI_Transceiving(uint16_t copy_u16Data)
{
	while(READ_BIT(SPI->SPI_CR1,BSY));
	SPI->SPI_DR=copy_u16Data;
	while(READ_BIT(SPI->SPI_CR1,BSY));

	return SPI->SPI_DR;
}
